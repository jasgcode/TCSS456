import React from "react";
import { useState } from "react";
import { useEffect } from "react";
// Declare the types for the properties of each chat
interface ChatProps {
    title: string;
    id: number;
    isActive?: boolean;
    variant?: "default" | "active";
}

const Chat = () => {


}